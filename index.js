const add = (x, y) => {
  return x + y;
};

const multiply = (x, y) => {
  return x * y;
}

console.log(add(5, 10));
console.log(multiple(5, 25));
const divide = (x, y) => {
  return x / y;
};

console.log(divide(5, 10));
